export interface DocumentType {
    id: number;
    documentName: string;
    documentType: string;
    status: boolean;
}

export interface PetSpecie {
    id: number;
    name: string;
    status: boolean;
}

export interface PetRace {
    id: number;
    name: string;
    status: boolean;
}
